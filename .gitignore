build/
dist/
dep/
*.o
*.d
*.a
*.so
*.dylib
*.vcvplugin
/*-test.vcv
metamodule/*.mmplugin
metamodule/metamodule-plugins/
.DS_Store
metamodule/.cache/

# throwaway composite panel previews (vcv-panel-gen-redux preview.py)
*.preview.svg
*.preview.html

# vcv/ distributables are copied from repo root at build time (see vcv/Makefile)
vcv/plugin.json
vcv/res/

# git worktrees for isolated feature work
.worktrees/

# archived standalone source repos (pre-merge history); see archive/README.md
archive/
__pycache__/

# SDD / subagent-driven-development scratch (ledgers, briefs, reports)
.superpowers/

# scratch prompts, not part of the repo
prompts/

# reproducible test-fixture renders (regenerated by tests/retours/mm_sim tooling)
tests/retours/mm_sim/*.wav

# root-level scratch patches and audio samples
/test-7.vcv
/looperman-l-2657941-0347670-doom-drum-loop.wav

# local-only scratch: DSP reference material (schematics/manuals/gerbers, not
# needed to build the plugin) and one-off demo patches / photos
/local/