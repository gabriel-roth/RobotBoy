# Round 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the round-3 "highest-value next steps" from `code-review-2026-07-08.md`: the remaining user-audible fixes, the Particules VCV batching perf win, a hardening/hygiene sweep, and test-lane wiring. No new features.

**Architecture:** Engine-level fixes go in the platform-agnostic classes (LoopEngine, EnginePool, beads_dsp) where they're unit-testable; module files only gain trivial overrides. The Particules batching change is a one-constant unification (block 64 on both hosts) plus its conditioning fallout.

**Tech Stack:** C++20; lanes: `tests/run.sh` (g++), `tests/beads/run.sh` (Catch2), `make -C vcv -j8`, `cmake --build metamodule/build -j8`. Python: `python3 -m unittest` (Task 6 wires it into the lane).

**Spec:** `docs/superpowers/plans/2026-07-09-round-3-spec.md`

## Global Constraints

- Branch `code-review-fixes`. One commit per task; messages one short sentence ≤15 words, NO Co-Authored-By or AI attribution.
- All four lanes green after every task.
- Never weaken an existing test's assertions or tolerances.
- No GUI/simulator steps — anything needing eyes/ears goes into the report's USER CHECK section instead.
- Sanctioned behavior changes only: Initialize clears the loop (Task 1); kill-fade/kill-order/mode-change scheduling (Task 2); seam wrap value with crossfade off (Task 3); VCV control granularity becomes once-per-64-samples + 64-sample I/O latency (Task 5). Everything else behavior-preserving.

---

### Task 1: Loooop/Lop onReset clears the loop

**Files:**
- Modify: `src/loooop/Loooop.cpp`, `src/loooop/Lop.cpp`

- [ ] **Step 1:** Add to both module structs, next to `onSampleRateChange`:

```cpp
    void onReset(const ResetEvent& e) override {
        Module::onReset(e);
        engine.clear();   // Initialize should silence the loop too (audio-safe; bumps the display revision)
    }
```

- [ ] **Step 2:** All four lanes. Module-level override; compile-verified (no headless Rack-module lane).
- [ ] **Step 3:** Commit: `fix: Initialize clears the recorded loop in Loooop and Lop`

---

### Task 2: beads grain-scheduler fix bundle

**Files:**
- Modify: `src/vendor/beads_dsp/src/grain/grain.h` (kill-fade), `grain_engine.h`/`grain_engine.cpp` (spawn-order kill), `grain_scheduler.h`/`grain_scheduler.cpp` (mode-change reset, dead state), `include/beads/parameters.h` (delete `clock_interval_samples`)
- Test: `tests/beads/` (new/extended: kill-fade, kill-oldest, first kClocked test)

**Interfaces:**
- Produces: `GrainEngine` gains a private `uint32_t spawn_serial_ = 0;` and `Grain` a `uint32_t spawn_serial;` stamped at activation. No public API change except the deleted `BeadsParameters::clock_interval_samples` (grep-verify zero readers/writers first — the review says the host never sets it).

- [ ] **Step 1 (TDD):** Write the failing tests first: (a) saturate the pool with DC-heavy content so the kill fallback engages; assert the fallback fade's amplitude sequence ends at exactly 0 (or assert no per-sample step > the pre-fix 0.25 bound in the rendered output); (b) fill the pool with grains spawned in a known order, force a kill, assert the earliest-spawned grain is the one killed (expose what's needed via existing test hooks or a minimal `#ifdef`-free accessor consistent with the codebase's test style); (c) kClocked: feed a regular clock, assert grains land on clock edges; switch gated→clocked mid-run and assert the first division is not off by one.
- [ ] **Step 2:** Run — RED for all three.
- [ ] **Step 3:** Implement: kill fade `(counter − 1)/kFallbackFadeSamples` (grain.h fallback block); spawn-serial tracking + lowest-serial victim selection in the kill fallback (with a comment on the ~24 h wraparound self-heal); `gate_phase_ = 0` (and any division counter) on trigger-mode change in the scheduler; delete `clock_period_` measurement/smoothing and `BeadsParameters::clock_interval_samples`.
- [ ] **Step 4:** GREEN; all four lanes.
- [ ] **Step 5:** Commit: `fix: grain kill fade reaches zero, oldest-grain victim, clocked-mode reset`

---

### Task 3: MF-20 stale-voice reset + Loooop seam fractional wrap

**Files:**
- Modify: `src/mf20/engine.hpp` (setVoices growth reset)
- Modify: `src/loooop/dsp/LoopEngine.cpp` (readInterpolated wrap target)
- Test: `tests/mf20/test_mf20.cpp`, `tests/loooop/test_loop_engine.cpp`

- [ ] **Step 1 (TDD, MF-20):** Test: ring voice index 1 hard (high res, hot input, K35 or OTA), `setVoices(1)`, run some samples, `setVoices(3)`, process silence through voices 1–2 and assert their first outputs are ~0 (not ringing). RED against current code.
- [ ] **Step 2:** Implement in `EnginePool`:

```cpp
    void setVoices(int n) {
        n = std::clamp(n, 1, 16);
        // Voices (re)entering the active range start clean — a voice that rang
        // at high resonance and went inactive must not re-emit its old state.
        for (int i = activeVoices; i < n; i++) engines[i].reset();
        activeVoices = n;
    }
```

Note `setVoices` runs every sample: the loop body only executes on growth (empty range otherwise). GREEN.

- [ ] **Step 3 (TDD, Loooop):** Test: record a short loop; set head size/position so `winStart` is fractional (e.g. loop length 10, size 0.55, centre 0.5 → verify the actual fractional winStart via the display snapshot or by construction); crossfade off; assert the wrap-adjacent read equals `(1-frac)·buf[i0] + frac·lerp(buf[floor(ws)], buf[ceil(ws)], ws-floor(ws))` rather than the truncated value. RED (current code truncates).
- [ ] **Step 4:** Implement: in `readInterpolated`, when `i1` would wrap past the window end, replace the truncated `buf[i1]` contribution with the fractionally-interpolated window-start value (two reads + lerp; guard `ceil` index by `loopLen_` like the existing clamps). Keep the crossfade-on path untouched (the seam crossfade previews via `readRaw` and is unaffected). GREEN; whole loooop suite must stay green unchanged.
- [ ] **Step 5:** All four lanes. Commit: `fix: reset re-activated MF-20 voices; fractional seam wrap target`

---

### Task 4: hardening + hygiene sweep

**Files:**
- Modify: `src/vendor/beads_dsp/src/buffer/recording_buffer.cpp` (fmod/isfinite), `src/vendor/beads_dsp/src/grain/grain_engine.cpp` (`ComputeGrainParams` isfinite fence; delete `render_load_tier_`/`kHighLoadActiveGrains`), `src/vendor/beads_dsp/src/fx/reverb.cpp` (+`reverb.h` if needed: Init guard), `src/vendor/beads_dsp/src/input/auto_gain.cpp` (kSilenceGain hoist), `src/vendor/beads_dsp/include/beads/types.h` (WavetableProvider/kWavetableSize), `src/vendor/beads_dsp/src/buffer/recording_buffer.h` (CopyRegionTo)
- Modify: `src/particules/Particules.cpp` (WARN on alloc failure)
- Modify: `src/loooop/Loooop.cpp`, `src/loooop/Lop.cpp` (`||` locals), `src/loooop/dsp/LoopEngine.cpp` (setJitter clamp; setSampleRate early-out)
- Modify: `src/mf20/engine.hpp` (named g-default constants)
- Modify: `Particules.md` (SEED position; quality-rate table)
- Test: `tests/beads/` (wrap-loop/fence behavior where cheap), existing suites guard the rest

Work through the spec's item-4 checklist in order; for each deletion grep repo-wide (excluding `.worktrees/`, `archive/`, build dirs) and record the grep in the report. Specific code points:

- [ ] **Step 1:** `setJitter` clamp on decrease: after `h.jitter = j;`, add `if (std::fabs(h.jitterNext) > 0.5f * j) h.jitterNext = (h.jitterNext < 0.f ? -0.5f : 0.5f) * j;` — rescale semantics: the pre-rolled offset may not exceed the new amount's range (offsets are `jitter · U(−0.5, 0.5)`).
- [ ] **Step 2:** `setSampleRate` early-out in the empty branch:

```cpp
    if (loopLen_ == 0 && !recording_) {
        if (sampleRate == sampleRate_ && bufL_.size() == maxSamples_ && maxSamples_ != 0)
            return;                       // already sized for this rate; skip the ~23 MB refill
        reset(sampleRate, maxSeconds_);
        return;
    }
```

- [ ] **Step 3:** Short-circuit fix in both modules:

```cpp
        bool recBtn  = recordBtn.process(params[RECORD_PARAM].getValue());
        bool recTrig = recordTrig.process(inputs[RECORD_TRIG_INPUT].getVoltage(), 0.1f, 2.f);
        if (recBtn || recTrig)
            engine.toggleRecord();
```

(and the same for clear).

- [ ] **Step 4:** The beads items (fmod/isfinite wrap, ComputeGrainParams fence, reverb Init guard, kSilenceGain file-scope hoist, orphan deletions incl. `render_load_tier_`), the MF-20 named constants, the Particules WARN, and the Particules.md corrections — per spec item 4. For the reverb guard: compute the required total from the existing alloc calls (or use the declared `kMinBufferSize` if it matches) and no-op Init (leave the reverb silent-but-safe) when the buffer is too small; add a Catch2 test constructing Reverb with a deliberately undersized buffer and asserting no crash and silent output.
- [ ] **Step 5:** All four lanes. Commit: `fix: defensive guards, dead-code sweep, RT-hygiene hoists, doc corrections`

---

### Task 5: Particules VCV batching (block 64 on both hosts)

**Files:**
- Modify: `src/particules/Particules.cpp` (`kWrapperBlockSize` unification — delete the `#ifdef METAMODULE` split)
- Modify: `src/particules/particules_cv_conditioning.h` (read first; `CvDecimationForBlock` ≥1 guard; verify the 64-block constants VCV will now inherit are exactly what MM uses today)
- Test: `tests/particules/test_cv_conditioning.cpp`, `tests/beads/test_control_conditioning.cpp`, `tests/beads/test_particules_block_runtime.cpp` — check for tests pinning VCV=1 semantics and extend/adjust ONLY by adding 64-block expectations (never weaken; if a test asserts block-1 passthrough as a property of the primitive, it stays).

- [ ] **Step 1:** Read `particules_cv_conditioning.h` and record in the report what `CvDecimationForBlock(1)` and `(64)` currently return and what each conditioner does with those values — the MM64 behavior is the reference; VCV must land exactly on it.
- [ ] **Step 2:** Unify:

```cpp
// One block size for both hosts: matches MetaModule exactly (identical CV
// conditioning cadence) and amortizes updateSlowParams/SetParameters ~64×
// on VCV. Costs 64 samples of I/O latency (1.3 ms @ 48 kHz).
static constexpr size_t kWrapperBlockSize = 64;
```

Add the `CvDecimationForBlock` ≥1 guard while touching the header.

- [ ] **Step 3:** Audit every per-sample vs per-block responsibility in `process()` against the spec's list (Schmitt gates, seed latch, quality button, output reads, grain-trigger pulse — all must remain per-sample; they already are). Record the audit in the report.
- [ ] **Step 4:** All four lanes. Add to the report a USER CHECK entry: play through Particules in VCV — dry/wet=0 passthrough feel, freeze-button responsiveness (≤64-sample quantization), CV modulation behavior matches MetaModule.
- [ ] **Step 5:** Commit: `perf: run Particules at a 64-sample block on VCV, matching MetaModule`

---

### Task 6: test wiring + coverage

**Files:**
- Modify: `tests/run.sh` (python lane), `tests/README.md`
- Modify: `tests/test_robotboy_identity.py` (slug parity)
- Create/extend: `tests/beads/test_nan_robustness.cpp` (NaN CVs → finite output), `tests/beads/test_feedback_path.cpp` (feedback=0 control case)
- Modify: `tests/beads/test_quality_modes.cpp:44`-area comment (5 kHz), `tests/beads/test_reverb.cpp` wow comment (0.02)

- [ ] **Step 1:** `tests/run.sh`: after the C++ loop, add a python stage (keep bash-3.2 compatibility — this script already had a `set -u` fix for stock macOS bash):

```bash
echo "== python guard tests =="
python3 -m unittest discover -s . -p 'test_*.py' -v || fail=1
```

- [ ] **Step 2:** Slug parity in `test_robotboy_identity.py`: load both `plugin.json` and `plugin-mm.json`, assert the ordered module-slug lists are equal.
- [ ] **Step 3:** NaN robustness (Catch2): init a `BeadsProcessor`, set every CV field in `BeadsParameters` to NaN over several blocks with audio input, assert all output samples finite. Depends on Task 4's fence — if it fails, the fence is incomplete: report, don't loosen.
- [ ] **Step 4:** `feedback = 0` control case + the two stale-comment fixes.
- [ ] **Step 5:** All four lanes (run.sh now includes python — confirm the two python tests actually run and pass). Commit: `test: wire python lane, slug parity, beads NaN robustness, feedback control case`

---

## Final verification

- [ ] All lanes green from a clean state (`rm -rf tests/beads/build` first).
- [ ] Update `code-review-2026-07-08.md`: prune round-3 items, refresh the categorized lists and next-steps section. Commit docs.
- [ ] Compile the accumulated USER CHECKS (display cache sim test, grain-overlap listening, Particules batching feel) into the final summary for the user.
