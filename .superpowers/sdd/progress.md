# Foobar combined plugin — progress ledger

Plan: ~/Dev/Loooop/docs/superpowers/plans/2026-07-05-foobar-combined-plugin.md

Task 1: complete (commit d2182f9, tree verified: 4 module sources, 4 SVGs, beads_dsp, native cores; no Ondes/Retours)
Task 2: complete (commit 126bef5, review clean/Approved — GATE PASSED: single init registers all 4, verified nm/readelf). Notes: (a) Particules.cpp needs bogaudio_widgets.hpp + res/bogaudio/*.svg — copy on VCV compile error; (b) Particules.cpp <malloc.h> needs guard for native macOS sim; (c) keep 3-mode guards METAMODULE_BUILTIN/FOOBAR_COMBINED/SIMULATOR intact; (d) spike already fixed ~8 vendored include paths.
Task 3: complete (vcv/Makefile + plugin.dylib build, all 4 models registered). Notes: (a) no bogaudio_widgets.hpp/SVGs needed after all — Particules.cpp uses only stock Rack widgets; (b) fixed 3 stale `../../src/...` includes in Loooop.cpp/Lop.cpp/LoopDisplay.hpp; (c) no type-name collisions, no namespacing needed; (d) plugin.mk's `rsync -rR` silently skips symlinked DISTRIBUTABLES on macOS — vcv/plugin.json and vcv/res/*.svg are hardlinks to the canonical files (works now, but degrades to independent copies after a fresh `git clone`; future edits to root plugin.json/res need manual re-sync into vcv/, or a small sync script); (e) Particules' constructor calls `APP->engine->getSampleRate()` (pre-existing, unchanged from source repo) — segfaults under the vcv-headless offline test host (no live engine Context), but loads/registers fine in the real Rack app.
Task 3: complete (commit c546a96, review clean/Approved). VCV plugin.dylib builds, all 4 register, real Rack logged "Loaded plugin Foobar 2.0.0". MINOR (for final review): vcv/plugin.json + vcv/res/*.svg are hard links to root (correct vs symlinks — openrsync skips symlinks — but drift across git clone; add build-time re-copy or CI drift check before release). Particules not headless-testable (constructor needs live engine; not a regression).
Task 4: complete. .mmplugin clean-rebuilds (single `init`, all symbols resolved). Verified all four modules in a built-in simulator build (metamodule/simulator, ext_builtin_brand_paths=Foobar): each instantiates, faceplate renders (screenshots), Loooop/Löp waveform-display draw call fires, and knobs respond (Loooop Speed-1 turned live to 8%; MF-20 param roller/panel-highlight tracked). Fixed a real bug: `Loooop_info.hh`/`Lop_info.hh`'s `png_filename` was missing the brand prefix (`create_plugin()` always nests assets under `<PLUGIN_NAME>/`, confirmed via `tar tf Foobar.mmplugin`) — changed to `"Foobar/Loooop/..."`; this affects the real .mmplugin too, not just the sim. `<malloc.h>`/`memalign` guards and `register.cc`'s `SIMULATOR` branch (from Task 2) worked unmodified. NOTE for Task 5: the brand-prefix hardcoded into those two `png_filename` strings must be updated if the brand slug changes, or faceplates go blank again.
Task 4: complete (commit ae07c14, review clean/Approved). MM .mmplugin rebuilds; all 4 verified in built-in sim (screenshots): instantiate, faceplates render (incl Löp ö), Loooop/Löp waveform draws, controls respond. Fixed real bug: native-core png_filename needed "Foobar/" prefix (create_plugin nests assets under brand). NOTE for Task 5: png_filename now "Foobar/Loooop/*.png" — keep in sync on any brand rename. Minor: Löp/Particules knob check shallower (same code path, low risk). Brand "Foobar" already applied in Task 2 (register_module) + Task 4 (png paths).
Task 5: complete — AUDIT ONLY, no source changes. Grepped `"Loooop"`/`Excellent Sounds`/`register_module<`/`brand`/`Brand` across src, metamodule, plugin.json, plugin-mm.json: every remaining `"Loooop"` hit is a module slug/symbol/asset-path segment (Loooop_info.hh slug, sync_info_positions.py module name, modelLoooop, assets/Foobar/Loooop/*), not the brand — Tasks 2+4 already set `register_module<...>("Foobar")` in both LoooopCore.cc:173 and LopCore.cc:136, and png_filename to "Foobar/Loooop/*.png". No "Excellent Sounds" occurrences anywhere in tracked source. Rebuilt both hosts clean: VCV `make -j` (no-op, already built, plugin.dylib exports all 4 Model* symbols); MM `cmake --build build` (Foobar.so single `init`, "All symbols found!", .mmplugin tar shows one top-level `Foobar/` dir holding plugin.json/plugin-mm.json + all 4 modules' assets). See task-5-report.md for full grep/adjudication.
Task 5: complete (commit 3221978, verified inline). Brand audit: nothing to fix — no "Excellent Sounds" anywhere; brand/slug/name = Foobar; native cores register("Foobar"); module slugs unchanged. Both hosts rebuild clean.
Task 6: complete. Ported DSP-level regression tests for all 4 modules into tests/{mf20,loooop,particules}: MF-20 test_mf20.cpp (30/30) + test_module_dsp.cpp (8/8), Loooop test_loop_engine.cpp + test_display_renderer.cpp (2 suites, all passing), Particules test_pitch_notch_map.cpp (all passing) — 201 total `ok`/`PASS` assertions across 5 binaries, `tests/run.sh` exits 0. Skipped: Loooop's `test/` dir (Python wiring/headless-sim tests, need a live host, not portable to the offline C++ harness); any test instantiating a Rack `Module` (Particules ctor needs `APP->engine`); a large Catch2 DSP suite found at `~/Dev/particules/nosuch_texture/tests/` (beads_dsp submodule: grain/delay/reverb/pitch/etc.) — out of the brief's named scope and needs a Catch2 toolchain not wired into this repo; noted as a good candidate for a future test-infra task. run.sh extended (documented, backward compatible) with an optional `<test>.cpp.extra` sibling file so Loooop's two tests can link their non-header-only LoopEngine.cpp/LoopWaveformRenderer.cpp; also fixed a bash-3.2 `set -u` empty-array crash on macOS's stock bash.
Task 6: complete (commit 5861b1d, verified inline: suite green exit 0, runner propagates failure correctly, no Ondes/Retours, diff only touches tests/). Ported DSP tests: MF-20 (test_mf20 + test_module_dsp), Loooop (test_loop_engine + test_display_renderer), Particules (test_pitch_notch_map). Skipped (documented): Loooop test/ Python/headless-sim tests, Rack-Module-instantiation tests (need live engine), beads_dsp Catch2 suite (out of scope, needs Catch2 toolchain).

ALL 6 TASKS COMPLETE. Open MINOR items for final-review triage:
- M1: vcv/plugin.json + vcv/res/*.svg hard-linked to root (correct vs symlinks per openrsync, but drift across git clone; add build-time re-copy or CI drift check before release).
- M2: png_filename in *_info.hh hardcodes "Foobar/" brand prefix — must update on real brand rename or faceplates go blank.
- M3: Löp/Particules got shallower sim knob-verification (same code path as Loooop/MF-20, low risk).
- M4: ledger has some duplicate task lines (cosmetic).
- M5: full interactive GUI (VCV) instantiate-and-click not done in sandbox (display asleep); worth a human check before release.

FINAL WHOLE-BRANCH REVIEW (opus): READY TO MERGE. No Critical/Important cross-cutting defects. Traced all 3 build modes symbol-by-symbol (single init per mode, no double-registration), guard scheme coherent, brand/asset/slug agree, build files correct (VCV compiles Loooop.cpp; MM uses native cores — no double-compile), VCV↔MM param-index parity holds. New Minor: duplicate top-level assets (unused); latent guard hazard if a future sim used libname "Loooop" (benign today). Pre-PUBLIC-RELEASE (not merge blockers): M1 vcv/ hardlink drift guard; M2 png_filename "Foobar/" coupling must change on brand rename (recommend a single brand constant). M3/M5 human smoke-test nice-to-have.

POST-REVIEW FIXES (user-requested):
- M2 done (commit 165bd8c): added metamodule/loooop/brand.hh with #define FOOBAR_BRAND; Loooop_info.hh/Lop_info.hh png_filename + both cores' register_module now derive from it. Rename sites reduced from ~5 to 2 (macro + plugin.json). MM rebuilds clean, asset nesting unchanged (Foobar/Loooop/*.png).
- M1 done (commit 28cedec): vcv/Makefile copies ../plugin.json + ../res into vcv/ at parse time (before plugin.mk reads SLUG); vcv/plugin.json + vcv/res/ now git-ignored + untracked (were drift-prone hard links). VCV clean-rebuilds, 4 Model symbols present, copies now independent (link count 1).

=== BEADS TEST LANE sub-project (branch beads-test-lane) ===
Task 1: complete (commit a23ea3e, review clean/Approved). Lane 2 stood up: tests/beads/ with vendored Catch2 v3.5.2 amalgamated + 2 shims, 16 upstream tests VERBATIM (0 diff), CMakeLists+run.sh. ctest green: 136 cases / 562,211 assertions, exit 0, offline, no test edits. Verified only tests/beads/ changed; lane 1 untouched. MINOR (final-review triage): (a) CMakeLists GLOB_RECURSE vs vcv/Makefile depth-1 wildcard — equivalent today, latent divergence; (b) redundant PRIVATE include dirs on beads_tests (inherited PUBLIC from lib).
Task 2: complete (commit 6070110, verified inline). Added "Test lanes" section to tests/README.md documenting lane 1 (g++ run.sh) + lane 2 (Catch2 beads run.sh, offline). Only tests/README.md changed.

FINAL WHOLE-BRANCH REVIEW (sonnet): READY TO MERGE. No blockers, no new cross-cutting issues. Verified no cross-repo build (all paths REPO_ROOT-relative), no network fetch, ctest failure propagation, build/ ignored, docs accurate. M1 (GLOB_RECURSE vs Makefile depth-1) + M2 (redundant include dirs) both acceptable follow-ups (inert/cosmetic). ALL TASKS COMPLETE.

=== CODE-REVIEW FIXES (branch code-review-fixes) ===
Plan: docs/superpowers/plans/2026-07-08-code-review-fixes-plan.md (7 tasks; 2→3→4 must be sequential)
Docs committed: 1960599
Task 1: complete (commits 1960599..c75460d, review clean/Approved). K35 loop clip + D1<=0 fallback verified algebraically by reviewer. Minor (final-review triage): (a) new tests print duplicate console numbers 19/20 (plan-mandated labels); (b) imprecise inline comment about "monotone" justification near D1 guard; (c) stability test detail buffer keeps only last failing combo.
Task 2: complete (commits c75460d..158a7d2, review clean/Approved). Process chunked into private ProcessBlock (single call site, structural <=64 invariant); pipeline order verified preserved. Minor (final-review triage): test_block_size.cpp DryParams leaves other params at defaults without a comment saying they're irrelevant to invariance.
Task 3: complete (commits 158a7d2..c8c9865, review Approved). prev_wet_buf per-sample feedback; read-before-write loop ordering independently verified. Plan's energy-ratio test observable was empirically invalid (held sample has higher crest power); replaced with roughness/energy staircase metric, red 0.0305 vs green 0.00078, threshold 0.00488 (geometric midpoint). Minor (final-review triage): (a) test has single hardcoded threshold, no fb=0 control case; (b) prev_wet_len clamp correctness relies on fixed per-host block size, not statically enforced in beads_processor.cpp.
Task 4: complete (commits c8c9865..241a58a, review clean/Approved). NotifyFreeze replaces broken Start/Process pair; wrap arithmetic + tail-mirror sync + fade<=size/2 overlap safety independently hand-verified; old API grep clean. Minor (final-review triage): (a) redundant static_cast<int>(kInterpolationTail); (b) unfreeze ramp g never hits exactly 1.0 inside the loop (write 33 is verbatim; equivalent, note only).
Task 5: complete (commits 241a58a..351d972, review clean/Approved). Pre-rolled jitter (jitterNext/commitJitter); preview/commit continuity traced algebraically both directions; jitter=0 bit-identical; RED 1.8485 -> GREEN 0.0175. Minor (final-review triage): (a) continuity test covers forward wraps only (plan-mandated; reverse path verified by review trace); (b) mid-fade setJitter/restartHead can still cause an intra-fade discontinuity (inherent to design, host-automation edge case).
Task 6: complete (commits 351d972..03d1625 + fix 1908b9c, re-review clean/Approved). Pitch quantize 0, SEED latch (per-sample, block-consume, VCV passthrough verified), block-aware conditioners; smoothing passthrough now bit-exact at block<=1. Minor (final-review triage): CvDecimationForBlock unguarded 8/block_size (UB at 0, unreachable today).
Task 7: complete. All lanes green from clean state (lane 1 all pass; lane 2 clean rebuild 100%; python 4/4; VCV plugin.dylib builds, only pre-existing Rack SDK header warnings).
FINAL WHOLE-BRANCH REVIEW (fable): READY TO MERGE. No Critical/Important. Cross-fix interactions verified (freeze+feedback in ProcessBlock, rapid freeze toggle, decimation+ramp, RT safety, VCV block=1 preservation claims re-derived). All 6 accumulated minors triaged: leave. New minors (post-merge follow-ups): jitterNext not rescaled on nonzero->nonzero jitter decrease; fb=0 control case for feedback test; CvDecimationForBlock guard. ALL 7 TASKS COMPLETE.

=== CODE-REVIEW FIXES ROUND 2 (branch code-review-fixes) ===
Plan: docs/superpowers/plans/2026-07-08-code-review-fixes-2-plan.md (6 tasks + final review)
Docs committed: 0cfd9c2
Task 1: complete (commits 0cfd9c2..bf4ced8, review clean/Approved). Minor noted: no multi-head/unchanged-rate setSampleRate test (final-review triage).
Task 2: complete (commits bf4ced8..0c0c90f, review clean/Approved). Minors noted: sanitize test poisons only hpFilter arm; ~2.5ms NaN latency is by-design (final-review triage).
Task 3: complete (commits 0c0c90f..733e3ed, review clean/Approved). Reviewer ⚠ LED colors resolved by controller: Particules.md:73-79 says white/cyan/amber/magenta, matches new code.
Task 4: complete (commits 733e3ed..051c6fd, review clean/Approved, opus reviewer verified steady-state g bit-equivalence). Sanctioned deltas: g-domain sweep smoothing, deterministic +-1e-9 dither.
Task 5: complete (commits 051c6fd..c6a1189, review clean/Approved, bit-identity traced against pre-refactor source incl. pre-commit display stores).
Task 6: complete (commits c6a1189..ef0c576, review clean/Approved, opus). Test-edit (output_index_ 1->0) verified legitimate dead-code assertion removal. Minor: RecordingBuffer::CopyRegionTo now orphaned (follow-up).
FINAL WHOLE-BRANCH REVIEW (fable): READY TO MERGE. No Critical/Important. Cross-task seams traced clean (minWinLen_ lifecycle, _sampleRate/g coherence, sanitize placement, CV-NaN clamp route, deletion fallout). Minors queued as round-3 follow-ups: magic-static hoists (kDecay/kSilenceGain), orphaned WavetableProvider/kWavetableSize + CopyRegionTo, named g-default constants, same-rate setSampleRate early-out, multi-head SR test. Fresh MM build re-verified exit 0. ALL 6 TASKS + FINAL REVIEW COMPLETE.

=== CODEX CARRY-OVER PLANNING (2026-07-09, docs only, no code) ===
Analyzed all 27 commits on review-fixes-codex vs code-review-fixes (3 parallel opus analyses: looper/MF-20/beads).
Carry: SVF denom cache (bad157c), MetaModule FTZ (cf74abc), CleanLoFi SoftClip bound (6754c75), MF-20 first-modulate+R-poly+drive smoothing (from 614a6af, re-designed zero-divide port), Loooop/Lop control-math header (806d935), waveform display cache stack (4f04486+7b3824d+LoopEngine hunks of d17e13d, -47%/-84% frozen raster), grain timing unification (ed7beb9, listen-test), random<1 (7b99f9d), LED SR decay (ee606e6), deferred ClearBuffer (c078e05 nugget), pitch map inline constexpr (32a051c nugget).
Skip (superseded/N-A): K35 clamp fbf834e, jitter 7e865ea, freeze 97e5c59, block sizes 83c26eb, memory growth 08627e4 (only needed under codex SR-scaled reverb), delay hoists 43bdcf0, dead-code chores.
Spec: docs/superpowers/plans/2026-07-09-codex-carryover-spec.md; Plan: same dir -plan.md (7 tasks). NOT EXECUTED.

=== CODEX CARRY-OVER EXECUTION (plan: 2026-07-09-codex-carryover-plan.md, 7 tasks; base 5497733) ===
CO Task 1: complete (commits 5497733..52853a7, review clean/Approved, byte-identical port of bad157c/6754c75/7b99f9d/32a051c-nugget).
CO Task 2: complete (commits 52853a7..8625f6c, review clean/Approved, FPSCR FZ+DN sequence verified incl. ARM disassembly by implementer).
CO Task 3: complete (commits 8625f6c..fb8e161, review clean/Approved, opus verified no audio-path sqrt/divide, no startup transient, cadence preserved). Minor: drive-threshold test covers OTA only (structurally mode-independent; final-review triage).
CO Task 4: complete (commits fb8e161..ab50fda incl. NaN-clamp fix ab50fda, re-review clean/Approved; clampSafe verified identical to rack::clamp for ALL inputs; MM cores also converted). Minor for final triage: loooop::clampSafe name collides with rack::math::clampSafe (different semantics, fully qualified so no ambiguity; consider rename).
CO Task 5: complete (commits ab50fda..8f8ff24, review clean/Approved, opus verified byte-identical renderer/display adoption, airtight invalidation, correct release/acquire). Deferred to USER: GUI-sim smoke test (canvas persistence) — see USER CHECK in task-5-report.md. Minor noted: per-sample dmb during recording (per-spec, honest cost).
CO Task 6: complete (commits 8f8ff24..1e70ee1, review clean/Approved, opus re-derived helper equivalence + block-coefficient identity; (b) exact only differs in high-load tier, sanctioned direction). Deferred to USER: listening check (dense cloud, sweep Density, no pumping/stepping). Minor for final triage: render_load_tier_ now write-only dead (kept per constraint; delete in future cleanup).
CO Task 7: complete (commits 1e70ee1..5068363 incl. Critical fix 5068363: onReset now reconfigures LED decay; re-review clean/Approved with behavioral pin test). Minors for final triage: invariant enforced by comment+pin rather than by construction; redundant final REQUIRE.
CO FINAL WHOLE-BRANCH REVIEW (fable): READY TO MERGE. No Critical/Important. Verified: revision-bump completeness vs setSampleRate no-bump path, FTZ vs dither (1e-9 is normal, survives FZ), SVF Tick callers all via setters, RNG fallout (NextExponential guard), MF-20 drive range [1,8] safe. Post-review cleanup 7bb84f3: grain_scheduler exponent hoisted to load time; FTZ re-arm on SR change. Follow-ups logged: batched revision bump (T5), render_load_tier_ deletion (T6), kSilenceGain still function-local static, deferred-clear-while-bypassed delay. USER CHECKS pending: GUI-sim canvas persistence (task-5-report.md), grain-overlap listening test (task-6-report.md). ALL 7 CO TASKS + CLEANUP COMPLETE.

=== ROUND 3 EXECUTION (plan: 2026-07-09-round-3-plan.md, 6 tasks; base = this docs commit) ===
R3 Task 1: complete (commits 47cfa35..58acdc8, review clean/Approved; controller amended out stray .pyc files + gitignored __pycache__).
R3 Task 2: complete (commits 58acdc8..ebb5913, review Approved, opus). Adjudications: fix (a) premise was WRONG in original review (old fade already reached 0; real click source = coarse 4-step fade, unchanged, out of scope); fix (b) kill-fallback UNREACHABLE in production (max_active cap silently drops overflow triggers first — new open question for doc); wraparound arithmetic corrected (994 days not 24h). Minors: victim loop lacks active() check; wrap comment slightly overstates. DOC UPDATE NEEDED at final: rewrite beads #1/#4 items accordingly.
R3 Task 3: complete (commits ebb5913..9628b32, review Approved; reviewer independently reproduced RED evidence bit-for-bit). Minors (report-wording only): crossfade-on path does exercise the new wrap branch at negligible weight; VoiceEngine reset leaves stale targets (bounded 2.5ms, per brief). Note: reviewer temporarily reverted hunks despite read-only instruction — tree verified intact afterward.
R3 Task 4: complete (commits 9628b32..2dc1af1, review Approved, opus; all 11 items verified, deletions grep-clean, auto_gain doubt resolved via standalone compile — kMinGainDb made public, no dupes). Minor residual: NaN pitch_ratio unfenced (ComputeGrainParams fence covers position only) — to be closed by Task 6 NaN test if RED.
R3 Task 5: complete (commits 2dc1af1..e490138, review clean/Approved, opus verified guard no-op, MM-match across conditioning/LED/latch/dry-mix, comb-risk excluded). USER CHECK queued: VCV feel test (passthrough, freeze responsiveness, CV match).
R3 Task 6: complete (commits e490138..cff03a9, review Approved, opus; adjudicated: SetParameters reverb change correct but NOT a pre-existing bug (fence-consequence, report overstated); NaN fences verified bit-identical for finite inputs; feedback=0 exact-zero baseline genuinely correct). Minors: had_output liveness guard satisfiable by dry path alone; report wording. New fences landed: pitch_ratio + SetParameters feedback/dry_wet/reverb.
R3 FINAL REVIEW (fable): found 2 Important cross-cutting (NaN density permanently silences scheduler phasor; NaN test masked it — never spawned grains). Fixed c669d1b (density/density_cv fence + liveness/recovery REQUIREs + VoiceEngine target reset), re-verified by reviewer: ALL FINDINGS CLOSED, READY TO MERGE. Open follow-ups logged in doc: grain-overflow design question, fence ordering, pitch-NaN direct test, kMidi tests. ALL 6 R3 TASKS + FINAL REVIEW + FIX COMPLETE.
USER CHECK 1 (display cache canvas persistence): PASSED, verified by user in built-in sim 2026-07-09.
USER CHECKS 2+3 (grain overlap listening; 64-block feel): PASSED, verified by user in VCV 2026-07-09. All manual checks complete.

=== MF-20 Q6 EXECUTION (plan: 2026-07-10-mf20-q6-plan.md, 3 tasks; base 0404c85) ===
Q6 Task 1: complete (34dec42, k35ForwardClip extracted as static helper, equivalence maxErr=0, all lanes green).
Q6 Task 2: complete (5b980c0, C1 quadratic knees w=0.1 on both corners, tests 25-27 green, existing K35 suite unchanged, all lanes green).
Q6 Task 3: complete (clean-state lanes green, one commit per task verified).
Q6 USER CHECKS (aliasing reduction at high drive+cutoff; clip character preserved): PASSED, verified by user in VCV 2026-07-10.

=== LOOOOP TRACK EXECUTION (plan: 2026-07-10-loooop-track-plan.md, 7 tasks; branch loooop-track; base 15d37c0) ===
Task 1: complete (commits 15d37c0..47092b8, review clean/Approved; Add-mode bit-exactness independently verified incl. no-fast-math check). Minors (final-review triage): (a) no multi-pass Layer/Decay compounding test; (b) writeFeedback() covers Add via default: case.
Task 2: complete (commits 47092b8..c5938a3, review clean/Approved, opus; every toggle state-machine path + ramp-index arithmetic independently traced). Minors (final-review triage): (a) no clear()/reset()-during-pending-stop test; (b) no stop-mid-up-ramp test; (c) clangd flags LoopEngine.cpp <algorithm> include now unused.
Task 3: complete (commits c5938a3..37650ff, review clean/Approved; all 5 choice-list sites identical, VCV/MM order parity + WriteMode enum-value alignment verified, bypass_routes untouched). Minor (final-review triage): Loooop_info.hh menu-only-elements comment not updated to include Write mode.
Task 4: complete (commits 37650ff..fa5ca8a incl. plan-correction 8fb514e, review clean/Approved, opus; all 3 sanctioned seam values re-derived independently, CR coefficients verified, wrap-loop termination proven via loopLen_>0 gate + minWinLen_>=1). MID-TASK PLAN FIX: implementer correctly refuted plan claim that test_subloop_window was invariant — controller sanctioned third expectation change (2.5 -> 2.375), plan updated + committed 8fb514e. Minor (final-review triage): tapWrapped boundedness comment could cite the loopLen_>0 gate.
Task 5: complete (commits fa5ca8a..0b1afb6, review clean/Approved; fade/retrigger arithmetic re-derived, 10 Hz degradation + looping-head no-op verified; implementer correctly re-placed fade block before fadeLen early-return, flagged not silent). Minors (final-review triage): (a) oneShotFadeLen duplicates fadeLen body minus the oneShot exclusion — consider shared helper; (b) untested corner: very short/fast one-shot window can compound retrigger ramp-in with end-fade.
Task 6: complete (commits 0b1afb6..0307e1b, review clean/Approved; all four hosts wired per scope, pan law untouched, levelSm guard placement verified, alpha saturation at 10 Hz confirmed). Minors (final-review triage): (a) duplicated cached-rate alpha-refresh idiom across the two VCV hosts; (b) LoooopCore panSm_ ctor-body init deviates cosmetically from brief sketch.
Task 7: complete. All lanes green from clean state (run.sh exit 0, vcv exit 0, MM .mmplugin built); RobotBoy not tracked in yml-to-vcv param_ranges.json (step moot); one commit per task, no attribution.
FINAL WHOLE-BRANCH REVIEW (fable): READY TO MERGE. No Critical/Important. All 3 sanctioned Q1 values re-derived again; cross-task seams (Replace-during-stop-ramp, Decay LP across re-arm, cubic-taps-vs-write-head, fade-over-cubic-endpoint, triple-gain stacking, MM parity/zero-init) all clean. Polish commit 0a06310 (3 comment fixes + MM-CPU USER CHECK added to plan). Follow-ups logged (not blocking): stale osRamp reset in triggerOneShot !playing path; mid-pass setWriteMode skips Decay LP seed; tests for clear/reset-during-stop-ramp + stop-mid-up-ramp; PRE-EXISTING: MM cores set_samplerate calls engine_.reset(sr) (destroys live loop) vs VCV setSampleRate (preserves) — backlog item. USER CHECKS pending (8 items incl. new MM CPU check). ALL 7 TASKS + FINAL REVIEW COMPLETE.

=== PARTICULES TRACK (plan: 2026-07-10-particules-track.md, 9 tasks; base aeb2e7c, branch particules-track) ===
Task 1: complete (commits aeb2e7c..440c3dc, review clean/Approved). Minors for final triage: (a) Catch2 tag naming consistency in test_particules_block_runtime.cpp (reviewer's claim of mixed tags looks mistaken — pre-existing tests already use [particules_block_runtime]); (b) StartGrainTriggerPulse(0)-during-pulse corner untested (benign).
Task 2: complete (commits 440c3dc..2bf2cc8, review clean/Approved). Minor for final triage: negative active_count falls through undefended (unreachable per ActiveGrainCount contract).
Task 3: complete (commits 2bf2cc8..10d1159, review clean/Approved). Minors for final triage: (a) +Infinity input renders "inf dB" (theoretical — InputLevel envelope can't realistically hit inf); (b) no just-above-floor boundary test (0.0011f).
Task 4: complete (commits 10d1159..f236d47, review clean/Approved; pitch_lock_ grep clean, threading contract verified at all 6 sites). Minors for final triage: (a) kPitchScaleModeCount unused; (b) particules_scales.h uses spaces vs tabs (brief artifact); (c) exclusivity ternary untested at wrapper level (per-plan compile+manual).
Task 5: complete (commits f236d47..5cedeb0, review clean/Approved). NEEDS_CONTEXT resolved by controller: plan's "Dry pass-through must pass unmodified" constraint contradicted the spec's decided test-flip; Option A implemented (tap unconditional on auto_gain), old SECTION converted to menu-off companion (dry_post_gain=false), 200-block settle sanctioned + reviewer-verified numerically. Minors for final triage: (a) settle-comment math wrong (~28% not 4% at 50 blocks; time constant 10.0k not 10.4k); (b) params_ alignment nit; (c) no auto_gain=true+dry_post_gain=true combined test (nondeterministic calibration, accepted). OWED: one manual sentence in Particules.md re post-gain dry not bit-clean at hot inputs (add at Task 9).
Task 6: complete (commits 5cedeb0..0448f49, review clean/Approved, opus; awake-path bit-identity, 12/12 flush, wake cleanliness all verified). No open findings (lfo_phase_ not flushed is intentional/energyless).
Task 7: complete (commits 0448f49..b9883ba, review clean/Approved; 7/7 wrap sites verified, 4 exclusions respected, json_t ownership traced no-leak). Minor for final triage: F&& mutate not std::forward'ed (harmless, plan-verbatim).
Task 8: complete (commits b9883ba..ef394ca, review clean/Approved; 33 pure renames @100%, repoint grep-clean incl. 3 sites beyond brief — test_cv_conditioning.cpp, test_no_delay_mode.py, tests/README.md; NOTICE.md verbatim). Minors for final triage: (a) tests/README.md:47 still says "vendored" (prose); (b) report miscounted 31 vs 33 renames (narrative only).
Task 9 (docs): complete (commit d79fcee — Particules.md feature docs incl. Q9 caveat sentence; tests/README.md wording).
FINAL VERIFICATION: g++ lane exit 0; Catch2 100% (157 cases); VCV plugin.dylib clean; MetaModule RobotBoy.mmplugin built, all symbols found.
FINAL WHOLE-BRANCH REVIEW (fable): READY TO MERGE. No Critical/Important. All 7 seams traced clean (F3 threading incl. release/acquire + undo replay; F8 JSON coherence; Q9×P1 bit-identity across sleep boundary via CosLookup(0)==1.0 exact; F5/F7 wrapper seam; P1 lifecycle incl. re-Init and setters-while-asleep; move grep-clean; MM ifdef seams). New minors (leave): 1-block pitch_scale_ race (matches existing UI→audio conventions, self-healing); stale tests/README.md binary count; settle-comment math (re-derived, conclusion correct). All accumulated task minors triaged LEAVE. USER CHECKS pending: plan Task 9 checklist items 1-7 + MM disabled-menu-item rendering + Root disabled-state refresh-on-reopen.
ALL 9 TASKS + FINAL REVIEW COMPLETE.

=== LOOOOP MENU REWORK (spec: 2026-07-10-loooop-menu-rework-design.md, plan: 2026-07-10-loooop-menu-rework-plan.md, 3 tasks; branch loooop-track, base 3173ca2) ===
Task 1: complete (per-head LoopEngine::setGridExclude + windowBounds gate; TDD RED->GREEN, 4 new checks, suite exit 0).
Task 2: complete (VCV: EXCLUDE_GRID per-head params, HEAD_PARAMS 8->9, "Head N" -> color playhead names, commands-first menu Trigger/V-Oct/Exclude-from-Grid; vcv build exit 0).
Task 3: complete (MM: QlpExcludeGridAlt zero-init-safe, Elements 87->91, updateHead XG template param, 4 call sites; MM build "All symbols found!", full lanes exit 0).
Executed inline (controller). USER CHECKS pending: 2026-07-10-loooop-menu-rework-user-checklist.md (4 items). Loooop.md updated for the new menu.

=== CODE-REVIEW ROUND 4 (plan: 2026-07-11-code-review-round-4-plan.md; Stream A on loooop-track @ c23ce94, Stream B on review-round-4 @ 1eb56fd) ===
Task 1 (A: osRamp): complete (commit 7d63539, review clean/Approved; base was 7f34c7f — user's own Löp panel commit landed on loooop-track before task start, not ours). Test B haveFirst guard adjudicated legitimate (first-sample silence->level artifact).
Task 1: complete (vcv-panel-gen 98081f8..1bcc6ce, review clean/Approved, 302 tests pass)
Task 2 (A: Decay LP seed): complete (commit 42dd120, review clean/Approved; base cb18fdc — user's own Löp polish docs commit intervened). Test adaptations setCrossfade(false)+restartHead(0) adjudicated honest by reviewer. Minors (final-review triage): (a) report says decayLpA_~0.5, actual ~0.544; (b) test comment '240-sample up-ramp' is 48kHz-specific.
Task 5 (B: grain steal-and-replace): complete (commits 70c8fa6..d57c6e3, review clean/Approved, opus; base 1eb56fd). Both saturation paths verified (CPU-cap-with-headroom pending-kill+free-slot; full-pool hard-replace), FindOldestActiveGrain excludes pending-kill, hook semantics preserved, Grain::Start() (grain.cpp:40-46) fully resets stale pending-kill/fallback state. Minors (final-review triage): (a) PLAN-MANDATED — multi-trigger block at CPU-cap can exceed cap by +N (not just +1) until victim fades finish; bounded/transient, consequence of the brief's sketch; note in design doc; (b) full-pool steal test doesn't assert ActiveGrainCount stays kMaxGrains (implied by the two existing asserts; coverage nicety).
=== RESUMED 2026-07-11 (fresh session): executing A3,A4 + B6-B12 per subagent-driven-development. ===
Task 12 (B: build/metadata cleanups): complete (commit f4f684a, base 090a143, review clean/Approved, sonnet). 6 items: (1) plugin.json +sourceUrl/manualUrl (JSON valid); (2) vcv/Makefile fail-loud sync ($(error)) + clean-escaped-objects rule (vcv/src only, live sources at repo root safe); (3) metamodule/CMakeLists.txt version derived via file(READ)+string(JSON) from plugin.json (cmake_minimum 3.22 already >=3.19, correctly not bumped), -std=c++20 scoped to $<$<COMPILE_LANGUAGE:CXX>:>; (4) presets/ removed (was empty/untracked), tests/beads/ never existed in this branch (no-op); (5) plugin-mm.json schema VERIFIED against metamodule-plugin-sdk/docs/plugin-mm-json.md (all keys correct) — left untouched; (6) all 3 verifications pass (dsp suite, Lane 1, VCV dylib) + real MM ARM toolchain configure confirmed CMAKE_PROJECT_VERSION=2.0.1 -> RobotBoy.mmplugin. ⚠️ RESOLVED by controller: git status clean, no stray build-verify dir. No DSP source touched. No Minors.
Task 11 (B: wrapper hygiene): complete (commit 090a143, base 43c7bda, review clean/Approved, sonnet).

ALL 12 TASKS COMPLETE (Stream A 1-4 on loooop-track; Stream B 5-12 on review-round-4, contiguous 70c8fa6..f4f684a, merge-base 1eb56fd). Stream A final review DONE (ready-to-merge).
STREAM B FINAL WHOLE-BRANCH REVIEW (opus, range 1eb56fd..f4f684a): READY TO MERGE (technical). No Critical/Important. All 4 cross-cutting risks PASS: (1) T5 steal-and-replace bounded by fixed 30-slot pool (absolute count never exceeds kMaxGrains; only relative to dynamic max_active cap); only over-cap consumer is LED brightness (clamped, harmless) + overlap gain-norm (more attenuation, correct); no double-steal (pending-kill excluded, hard-replace victim becomes highest serial); spawn_serial monotonic; Grain::Start fully resets kill state so hard-replaced slots inherit nothing. (2) T5/T6 independent functions, T6 fence now also protects steal-path grains. (3) T9/T10 different classes/files, no helper/constant collision. (4) control_conditioner.h reachable on ALL 4 build include paths (VCV Makefile, MM CMake, Catch2 test CMake, Lane-1 run.sh), not just the VCV build tested. Minors (all previously tracked, no cross-cutting escalation): recording_buffer.CPP dead <cmath> (NOTE: the .H one is CORRECT — ResolveReadPosition inlines fmod/isfinite there); T5 multi-trigger over-cap +N (plan-mandated). Reviewer recommends folding the dead-.cpp-cmath removal into a cleanup commit (cosmetic, safe) — LEFT for user (out of plan-task scope, commit-only-when-asked).
BOTH STREAMS COMPLETE + FINAL-REVIEWED READY-TO-MERGE. Neither merged (user's call; loooop-track gates on pending USER CHECKS).
DOC UPDATE DONE: code-review-2026-07-08.md on main updated (working tree, UNCOMMITTED per commit-only-when-asked) — 15 round-4 done markers; Loooop follow-ups #1-3, Loooop test gaps, Particules #1 + refactorings, dsp lib #1/#3 + refactorings + test gaps, plugin-glue 1-4 all struck-through/done; in-flight branches + still-to-do/next-steps sections updated; plugin-mm.json noted VERIFIED (not unverifiable). Left OPEN (out of round-4 scope): MF-20 bugs #1/#2, dsp lib #2 NextGaussian, perf/waveform-bump, optional DSP-quality features. ROUND 4 FULLY COMPLETE. 3 fixes: (1) particules_density_control.h param renamed conditioned_density_cv->density_cv_volts + comment "raw ±5V", 3 unused includes dropped, *0.2f math byte-unchanged, no caller change (positional); (2) control_conditioner.h git mv to dsp/include/particules_dsp/ as 100%-similarity pure rename, all 3 includers updated (Particules.cpp:3, test_control_conditioning.cpp:4, test_cv_conditioning.cpp:2), grep-clean of old path; (3) onReset clear_requested_.store(false) at Particules.cpp:254 before ClearBuffer() (closes stale-clear window, doesn't disturb exchange consumer :442 or menu producer :711). dsp suite pass + VCV dylib clean (pre-existing -Wdeprecated-this-capture from Rack SDK helpers.hpp only). Minors (final-review triage): (a) commit msg dense (13 words, passes); (b) warnings confirmed unrelated.
Task 10 (B: RecordingBuffer read-path consolidation): complete (commit 43c7bda, base 24f099c, review clean/Approved, sonnet). Behavior-preserving: deleted dead ReadLinearStereoFast (grep-confirmed 0 callers), extracted ResolveReadPosition prologue (guard+fmod-wrap+split), rewrote ReadHermite/ReadHermiteStereo/ReadLinear to call it with tap-math preserved; ReadHermiteStereo channels_<2 guard preserved (reordered, behaviorally invisible - 3 independent short-circuits to same zero output); hot-path ReadHermiteStereoFast byte-untouched (grain.h:69 caller unmodified). Suite 165 cases pass unchanged. Minor (final-review triage / quick cleanup candidate): recording_buffer.cpp:5 `#include <cmath>` now genuinely dead (only std::memset+std::min remain, covered by cstring/algorithm) — reviewer verified real, not clangd false positive; remove for cleanliness (compiles via transitive today, not guaranteed on all stdlibs).
Task 9 (B: quality-mode crossfade dedup): complete (commit 24f099c, base fd626af, review clean/Approved, sonnet). Behavior-preserving refactor: extracted ApplyModeXfade(int& counter, input, result) helper, both ProcessInput/ProcessOutput call sites replaced. Reviewer verified arithmetic token-identical to both originals (guard, cast order, blend operand order, post-decrement); int& correctly reproduces in-place member decrement. No tests changed; existing suite 165 cases pass unchanged. Minor (final-review triage): helper is only header-inline method in the class (brief-prescribed, not a regression).
Task 8 (B: interpolation-tail sync test): complete (commit fd626af, base cd7271f, review clean/Approved, sonnet). TEST-ONLY (diff only test_buffer.cpp; recording_buffer.cpp restored byte-identical). Writes full 64-frame ramp then 2 more (write_head 0,1 < kInterpolationTail) with sentinels 0.9/0.8; read at 63.5 taps i1=64,i2=65 hit tail mirrors; expected via InterpolateHermite; fast+out-of-line readers asserted to agree. Prove-it-bites RED (0.31563 vs 0.7719 stale) then GREEN 165/165. Minor (final-review triage): doesn't also cover ReadHermite single-ch / ReadLinearStereoFast (out of brief scope; latter is dead, deleted by Task 10).
Task 7 (B: pitch-NaN grain test): complete (commit cd7271f, base e26c009, review clean/Approved, sonnet). TEST-ONLY (diff touches only test_grain.cpp; fence intact in committed tree). NaN pitch_cv liveness+recovery test; prove-it-bites confirmed RED (ActiveGrainCount 0>0 with fence removed) then GREEN. ⚠️ field-name check RESOLVED by controller: pitch_ar/pitch_cv/pitch_cv_connected all real in parameters.h:27,33,37. Minor (final-review triage): recovery window re-asserts ActiveGrainCount but not all_finite (per brief snippet).
Task 6 (B: grain position fence ordering): complete (commit e26c009, base d57c6e3, review clean/Approved, sonnet). Fence moved above the reverse-branch while-loop (single fence, no duplicate, no redundant post-span guard). IMPLEMENTER CORRECTLY REFUTED BRIEF: `reverse` derives from params.size < kSizeBoundary(-0.2f), NOT pitch sign — test adapted to size=-0.5f per brief's own fallback; reviewer confirmed size=-0.5f -> reverse=true via source (attenurandomizer early-returns at size_ar=0 default), so test genuinely exercises the path. Minors (final-review triage): (a) test sets params.pitch=0 redundantly (struct default); (b) test can't fail pre-fix (upstream fences), regression-guard only, by design per brief.
Task 3 (A: MM set_samplerate preserve): complete (commit bf28267, base f5366e9, review clean/Approved, sonnet). Both cores' set_samplerate now call engine_.setSampleRate(sr) (not reset), smoother-alpha lines intact; ctor reset(48000) out of scope. Two new breadth tests registered+real (multi-head/nondefault-speed survives retune; redundant same-rate no-op hits LoopEngine.cpp:48-49 early-return). Core-level test correctly skipped (test_module_dsp.cpp includes only header-only LooperModuleDSP.hpp, no .extra recipe). Minors (final-review triage): (a) multi-head retune asserts coarse (finite+energy>0.1, per brief); (b) same-rate test misses the nothing-recorded early-return path.
Task 4 (A: stop-ramp corner tests): complete (commit 74b80a1, base 50ee90a, review clean/Approved, sonnet). TEST-ONLY, no defects found. 3 tests pin: clear()/reset() mid-stop-ramp, record-off mid-up-ramp — reviewer traced state machine, all genuinely reach the corner (up-ramp saturates at 240 xfade samples; mid-down-ramp odGain~0.583; mid-up-ramp odGain~0.417). setCrossfade(false) omission verified harmless (crossfade_ only gates playback seam declick, not write-gain ramp). Minors (final-review triage): (a) "finite after clear/reset" assert structurally guaranteed by loopLen_>0 gate (low signal, from brief); (b) peak<=2.0 bound loose vs ~1.42 actual; (c) reset test has less follow-up than clear test (brief template asymmetry).
STREAM A (Tasks 1-4) COMPLETE — all reviewed clean. Round-4 task commits: 7d63539(T1) 42dd120(T2) bf28267(T3) 74b80a1(T4), interleaved with user's parallel Löp commits on loooop-track.
STREAM A FINAL WHOLE-BRANCH REVIEW (opus, scoped to our 4 commits): READY TO MERGE (technical). No Critical/Important, no new Minors. All 3 cross-cutting risks CLEAR: (1) T1 osRamp (per-head read-path gain) and T2 decayLp/odGain (engine write-path state) are provably independent, setWriteMode out-lining kept writeMode_=m; T1's !playing reset also covers clear() for free (clear sets playing=false). (2) T3 VCV↔MM parity complete (both cores mirror onSampleRateChange; no init regression — ctor still reset(48000), setSampleRate falls back to reset when empty; LopCore is single-head engine_(1)); (3) setWriteMode out-line link-safe (sole non-test caller OverdubControl.hpp:27; all TUs link LoopEngine.cpp). MERGE GATES ON PENDING USER CHECKS (not my call).
HANDOFF NOTES (2026-07-11, for a fresh session/model):
- Plan (12 tasks, 2 streams): docs/superpowers/plans/2026-07-11-code-review-round-4-plan.md. Execute per superpowers:subagent-driven-development. Source doc being closed out: code-review-2026-07-08.md ("Still to do" section).
- Stream A = Loooop, worktree .worktrees/loooop-track (branch loooop-track). Done: Task 1 (7d63539), Task 2 (42dd120), both reviewed clean. Next: Task 3 (MM set_samplerate preserve + SR breadth tests), then Task 4. CAUTION: user commits to this branch in parallel (7f34c7f, cb18fdc, 88af932 are theirs) — always set review-package BASE to the commit actually preceding the task's own commits.
- Stream B = particules_dsp/wrapper/build, worktree .worktrees/review-round-4 (branch review-round-4, base 1eb56fd). Task 5 implemented (70c8fa6+d57c6e3), suite green, NOT YET REVIEWED — first action on resume: scripts/review-package 1eb56fd d57c6e3, dispatch task reviewer per task-reviewer-prompt.md. Then Tasks 6-12 in order.
- Task briefs/reports: .superpowers/sdd/task-N-brief.md / task-N-report.md (briefs for 3,4,6-12 not yet extracted — use scripts/task-brief PLAN N from the subagent-driven-development skill dir).
- After all tasks: final whole-branch review per stream, then update code-review-2026-07-08.md on main (mark implemented items done). Do NOT merge either branch (user's call; loooop-track gates on pending USER CHECKS).
