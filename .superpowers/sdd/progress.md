# Foobar combined plugin — progress ledger

Plan: ~/Dev/Loooop/docs/superpowers/plans/2026-07-05-foobar-combined-plugin.md

Task 1: complete (commit d2182f9, tree verified: 4 module sources, 4 SVGs, beads_dsp, native cores; no Ondes/Retours)
Task 2: complete (commit 126bef5, review clean/Approved — GATE PASSED: single init registers all 4, verified nm/readelf). Notes: (a) Particules.cpp needs bogaudio_widgets.hpp + res/bogaudio/*.svg — copy on VCV compile error; (b) Particules.cpp <malloc.h> needs guard for native macOS sim; (c) keep 3-mode guards METAMODULE_BUILTIN/FOOBAR_COMBINED/SIMULATOR intact; (d) spike already fixed ~8 vendored include paths.
Task 3: complete (vcv/Makefile + plugin.dylib build, all 4 models registered). Notes: (a) no bogaudio_widgets.hpp/SVGs needed after all — Particules.cpp uses only stock Rack widgets; (b) fixed 3 stale `../../src/...` includes in Loooop.cpp/Lop.cpp/LoopDisplay.hpp; (c) no type-name collisions, no namespacing needed; (d) plugin.mk's `rsync -rR` silently skips symlinked DISTRIBUTABLES on macOS — vcv/plugin.json and vcv/res/*.svg are hardlinks to the canonical files (works now, but degrades to independent copies after a fresh `git clone`; future edits to root plugin.json/res need manual re-sync into vcv/, or a small sync script); (e) Particules' constructor calls `APP->engine->getSampleRate()` (pre-existing, unchanged from source repo) — segfaults under the vcv-headless offline test host (no live engine Context), but loads/registers fine in the real Rack app.
Task 3: complete (commit c546a96, review clean/Approved). VCV plugin.dylib builds, all 4 register, real Rack logged "Loaded plugin Foobar 2.0.0". MINOR (for final review): vcv/plugin.json + vcv/res/*.svg are hard links to root (correct vs symlinks — openrsync skips symlinks — but drift across git clone; add build-time re-copy or CI drift check before release). Particules not headless-testable (constructor needs live engine; not a regression).
Task 4: complete. .mmplugin clean-rebuilds (single `init`, all symbols resolved). Verified all four modules in a built-in simulator build (metamodule/simulator, ext_builtin_brand_paths=Foobar): each instantiates, faceplate renders (screenshots), Loooop/Löp waveform-display draw call fires, and knobs respond (Loooop Speed-1 turned live to 8%; MF-20 param roller/panel-highlight tracked). Fixed a real bug: `Loooop_info.hh`/`Lop_info.hh`'s `png_filename` was missing the brand prefix (`create_plugin()` always nests assets under `<PLUGIN_NAME>/`, confirmed via `tar tf Foobar.mmplugin`) — changed to `"Foobar/Loooop/..."`; this affects the real .mmplugin too, not just the sim. `<malloc.h>`/`memalign` guards and `register.cc`'s `SIMULATOR` branch (from Task 2) worked unmodified. NOTE for Task 5: the brand-prefix hardcoded into those two `png_filename` strings must be updated if the brand slug changes, or faceplates go blank again.
Task 4: complete (commit ae07c14, review clean/Approved). MM .mmplugin rebuilds; all 4 verified in built-in sim (screenshots): instantiate, faceplates render (incl Löp ö), Loooop/Löp waveform draws, controls respond. Fixed real bug: native-core png_filename needed "Foobar/" prefix (create_plugin nests assets under brand). NOTE for Task 5: png_filename now "Foobar/Loooop/*.png" — keep in sync on any brand rename. Minor: Löp/Particules knob check shallower (same code path, low risk). Brand "Foobar" already applied in Task 2 (register_module) + Task 4 (png paths).
Task 5: complete — AUDIT ONLY, no source changes. Grepped `"Loooop"`/`Excellent Sounds`/`register_module<`/`brand`/`Brand` across src, metamodule, plugin.json, plugin-mm.json: every remaining `"Loooop"` hit is a module slug/symbol/asset-path segment (Loooop_info.hh slug, sync_info_positions.py module name, modelLoooop, assets/Foobar/Loooop/*), not the brand — Tasks 2+4 already set `register_module<...>("Foobar")` in both LoooopCore.cc:173 and LopCore.cc:136, and png_filename to "Foobar/Loooop/*.png". No "Excellent Sounds" occurrences anywhere in tracked source. Rebuilt both hosts clean: VCV `make -j` (no-op, already built, plugin.dylib exports all 4 Model* symbols); MM `cmake --build build` (Foobar.so single `init`, "All symbols found!", .mmplugin tar shows one top-level `Foobar/` dir holding plugin.json/plugin-mm.json + all 4 modules' assets). See task-5-report.md for full grep/adjudication.
Task 5: complete (commit 3221978, verified inline). Brand audit: nothing to fix — no "Excellent Sounds" anywhere; brand/slug/name = Foobar; native cores register("Foobar"); module slugs unchanged. Both hosts rebuild clean.
Task 6: complete. Ported DSP-level regression tests for all 4 modules into tests/{mf20,loooop,particules}: MF-20 test_mf20.cpp (30/30) + test_module_dsp.cpp (8/8), Loooop test_loop_engine.cpp + test_display_renderer.cpp (2 suites, all passing), Particules test_pitch_notch_map.cpp (all passing) — 201 total `ok`/`PASS` assertions across 5 binaries, `tests/run.sh` exits 0. Skipped: Loooop's `test/` dir (Python wiring/headless-sim tests, need a live host, not portable to the offline C++ harness); any test instantiating a Rack `Module` (Particules ctor needs `APP->engine`); a large Catch2 DSP suite found at `~/Dev/particules/nosuch_texture/tests/` (beads_dsp submodule: grain/delay/reverb/pitch/etc.) — out of the brief's named scope and needs a Catch2 toolchain not wired into this repo; noted as a good candidate for a future test-infra task. run.sh extended (documented, backward compatible) with an optional `<test>.cpp.extra` sibling file so Loooop's two tests can link their non-header-only LoopEngine.cpp/LoopWaveformRenderer.cpp; also fixed a bash-3.2 `set -u` empty-array crash on macOS's stock bash.
